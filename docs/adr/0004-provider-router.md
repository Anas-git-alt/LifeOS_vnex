# ADR 0004: Provider Router

Status: accepted

Provider/model configuration is per-agent. OpenRouter and NVIDIA NIM are normal chat/model providers with key and provider fallback. Codex OAuth is initially scoped to coding and Systems/DevOps workflows.

All provider calls must emit traceable status and telemetry.
